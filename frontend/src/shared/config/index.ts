export const REFRESH_INTERVAL_MS = 5000;
export const API_BASE = "";

export const ROUTES = {
  dashboard: "/",
  keys: "/keys",
  workspaces: "/workspaces",
  accounts: "/accounts",
  settings: "/settings",
} as const;
